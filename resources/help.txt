##SQLITE
foreign keys en SQLite necesitan activarse explícitamente con
PRAGMA foreign_keys = ON;
Pragma table_info()


model_dump(exclude_unset=True) — esto te da un diccionario solo con los campos que el usuario mandó explícitamente,


CREATE TABLE tareas (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    titulo TEXT,
    descripcion TEXT,
    fecha TEXT,
    estado TEXT,
    category_id INTEGER,
    FOREIGN KEY (category_id) REFERENCES categorias(id) ON DELETE CASCADE
);

##POSTGRES

Mirar esquema tabla -> \d tabla


IndexError: tuple index out of range
Este error tiene que ver con la cantidad de parametros pasados a la query de sql a traves de psycopg2
en la linea cursor.execute(query, listparams) ej: 3 params 4 objetos en la lista